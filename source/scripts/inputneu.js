/*
let perceptron;
function setup() {
    perceptron = new Perceptron();
    const inputs = [-1, 0.5]

    const guess = perceptron.guess(inputs);

    setTimeout(() => {
        setup()
    }, 1000)

}
*/